#!/usr/bin/env bash

# Inherit the parent .envrc (devbox + AWS_PROFILE).
source_up

# Add this directory's bin/ to PATH so the `tf` shortcut is available
# only while inside eks-platform.
PATH_add ./bin
